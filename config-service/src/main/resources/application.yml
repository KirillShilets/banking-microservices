spring:
  cloud:
    config:
      server:
        native:
          search-locations: file:/app/config
      enabled: false
  profiles:
    active: native
  security:
    user:
      password: bankconfigadmin123

server:
  port: 8001