spring:
  application:
    name: account-service
  config:
    import: "configserver:http://config-service:8001"
  cloud:
    config:
      fail-fast: true
      username: user
      password: bankconfigadmin123